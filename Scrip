--[[ 
  SCRIPT DE PRUEBA DEL HUD (LIBRARY)
  Descarga y ejecuta el HUD, y agrega unos botones de muestra para ver el diseño, el scroll, y los callbacks.
  SOLO PARA PRUEBA VISUAL. No incluye menús complicados.
--]]

-- Pega el RAW-LINK de tu Hud.lua aquí 👇
local HUD_URL = "https://raw.githubusercontent.com/inmortales-likes-bot/Lua/refs/heads/main/Hud"

-- ↓↓↓ No edites nada abajo salvo para cambiar los botones de ejemplo ↓↓↓

local ok, code = pcall(function() return game:HttpGet(HUD_URL) end)
if not ok or not code or code == "" then
    warn("No se pudo descargar el HUD. Comprueba el enlace RAW.")
    return
end

local ok_load, loadErr = pcall(function() loadstring(code)() end)
if not ok_load or not _G.LuaLibrary then
    warn("No se pudo inicializar la librería HUD. ¿Exporta _G.LuaLibrary?")
    return
end

-- Crear el HUD principal:
local HUD = _G.LuaLibrary.new()

-- Sección Scroll con muchos botones:
local scrollSec = HUD:addSection("Scroll Demo (10 Botones)")
for i = 1, 10 do
    scrollSec:addButton("Botón #" .. i, function()
        print("Presionaste el Botón #" .. i)
    end)
end

-- Sección básica:
local baseSec = HUD:addSection("Ejemplo Básico")
baseSec:addButton("Hola Mundo", function() print("¡Hola mundo!") end)
baseSec:addButton("Cerrar HUD", function()
    if HUD.ScreenGui then HUD.ScreenGui:Destroy() print("HUD Cerrado.") end
end)

print(">> HUD DE PRUEBA LISTO <<")
